	#include <time.h>
	#include <stdio.h>
	#include "E101.h"
		#include <time.h>
	#include <stdio.h>
	#include "E101.h"
	
	int main (){
	int main (){
		
		// if cannot detect white line
		if(){
			//function which scans for objects instead of white line
			}
		else{
			//function which scans for white line
			}
		}
